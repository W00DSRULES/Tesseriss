name: tests

on:
  push:
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: unit + UI tests
    runs-on: macos-15
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Install xcodegen
        run: brew install xcodegen

      - name: Generate Xcode project
        run: xcodegen generate

      - name: Pick simulator
        id: sim
        run: |
          set -euo pipefail
          # Prefer any modern iPhone simulator that's available on this runner.
          DEVICE=$(xcrun simctl list devices available -j \
            | python3 -c 'import json, sys; data = json.load(sys.stdin)["devices"]; \
                          best = next((d["name"] for runtime, devs in data.items() if "iOS" in runtime \
                                     for d in devs if d.get("isAvailable") and "iPhone" in d["name"]), None); \
                          print(best or "iPhone 16")')
          echo "device=$DEVICE" >> "$GITHUB_OUTPUT"
          echo "Picked simulator: $DEVICE"

      - name: Boot simulator
        run: |
          set -euo pipefail
          # Pre-boot so the first UI test doesn't hit a cold simulator
          # ("Failed to get background assertion" flake at app launch).
          xcrun simctl boot "${{ steps.sim.outputs.device }}" || true
          xcrun simctl bootstatus "${{ steps.sim.outputs.device }}" -b

      - name: Run unit + UI tests
        run: |
          set -o pipefail
          # ScreenshotTests captures App Store marketing shots — not a
          # correctness test (see its doc comment); run it manually instead.
          xcodebuild test \
            -project Tesseriss.xcodeproj \
            -scheme Tesseriss \
            -destination "platform=iOS Simulator,name=${{ steps.sim.outputs.device }},OS=latest" \
            -skip-testing:TesserissUITests/ScreenshotTests \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO
